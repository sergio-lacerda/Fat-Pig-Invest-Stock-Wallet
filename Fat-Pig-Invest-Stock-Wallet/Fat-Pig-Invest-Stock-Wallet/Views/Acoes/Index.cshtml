@model IEnumerable<Fat_Pig_Invest_Stock_Wallet.Models.Acao>

@{
    ViewData["Title"] = "Ações";
}

<h1>Ações</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Adicionar Ação</a>
</p>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ticker)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ticker)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa.Nome)
            </td>
            <td class="td-botoes-table">
                <a class="badge bg-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                <a class="badge bg-success" asp-action="Details" asp-route-id="@item.Id">Detalhes</a>
                <a class="badge bg-danger" asp-action="Delete" asp-route-id="@item.Id">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
